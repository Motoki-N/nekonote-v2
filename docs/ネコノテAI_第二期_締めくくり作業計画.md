# ネコノテAI 第二期 締めくくり作業計画

- 作成日: 2026-08-07
- 素材: 締めくくり作業指示書（ユーザー作成）を、リポジトリの実状（2026-08-07 時点・セッション80まで完了）と突き合わせて補正したもの
- 進め方: 各ステップは Interview → SPEC / チェックリスト → 新規セッション のサイクルで実施。**二失敗ルール適用**（同じアプローチで2回失敗したら手を止めて人間に相談する）
- 方針: **コードを変える作業を先に完了させ、コードに依存する資料を後に作る**

---

## 0. 指示書からの補正点（実状との突き合わせ結果）

| # | 指示書の記述 | 実状 | 計画への反映 |
|---|---|---|---|
| 1 | Step 0「TESTING.md のシードデータで回帰テスト」 | 第二期には TESTING.md が存在しない。シードデータ機構もない（標準シードはマイグレーション内の personas 7・review_profiles 9・templates 4 のみ）。**第一期リポジトリ側には `docs/TESTING.md`（514行）が実在**し、指示書はこれを指していたと判断 | Step 0 の最初に回帰テストチェックリスト `docs/TESTING.md` を新規作成する（第一期版を雛形に、各 SPEC の E2E 節＋manual.md から集約） |
| 2 | Step 3「SCHEMA.md / API規約 / ADR / DESIGN.md を突き合わせ」 | 第二期には無い。**第一期リポジトリの資料構成（DATABASE.md / API.md / ARCHITECTURE.md ほか計7本・4,357行）を指していた**もの。`docs/adr/` もどちらにも無い | 突き合わせ対象を実在ドキュメント（SPEC 26本・README・運用計画・実装計画・要求仕様・manual.md）に読み替え |
| 3 | Step 5「外部向けユーザーマニュアル作成」 | `docs/manual.md`（529行・2026-08-05 更新・§14まで）が既に存在。ただし §12 保守対応を含み、スクリーンショットは無し | 新規作成ではなく**既存 manual.md の外部向け改訂**に変更 |
| 10 | （記載なし） | 第一期リポジトリ（`<v1-repo>`）がローカル・GitHub 双方に現存し、`v0.1.0` タグ付きで比較可能な状態。指示書の Step 8 には第一期との比較観点が無かった | **Step 8 に「第一期との比較」を追加**（機能・性能・開発の進め方の3観点）。実測ベースラインは Step 8 に記載 |
| 4 | Step 6「CHANGELOG 更新」 | CHANGELOG は存在しない。git タグも1つもない（v0.1.0 タグ無し・package.json は 0.1.0） | CHANGELOG は新規作成。v0.2.0 タグ付け時に package.json のバージョンも 0.2.0 へ同期 |
| 5 | Step 7「docs/adr/ を素材に」 | ADR は無い。代わりに dev-log.md（80セッション分）・incident-log.md・運用計画 §2（第一期KPT→第二期対応表）が豊富 | 素材リストを実在資料へ差し替え（dev-log / incident-log は指示書に無かったが最重要素材） |
| 6 | Step 8「series_id / volume_number / scope の仕込み状況を整理」 | スキーマ・コードには一切存在しない。dev-log に設計インタビューの確定方針（6項目）が記録されているのみ | 「実装済みの仕込み」ではなく「**設計確定・実装未着手**」の現状を1枚に整理する作業として定義 |
| 7 | Step 2「GitCredentialProvider 経由の徹底」 | `lib/git/credentials.ts` に型 `GitCredentialProvider`＋実装 `patCredentialProvider` が実在。記述どおり | そのまま。参考: 前回監査 `docs/security-audit-20260714.md`。npm audit 現状 20件（high 10 / moderate 10、主に vivliostyle 系依存の valibot） |
| 8 | （記載なし） | リポジトリは現在 PRIVATE。公開化の準備（SECURITY.md・LICENSE・伏せ字規約・CLAUDE.md の公開前提化）は完了済み | **Step P（公開化）を新設**し、Step 2 完了直後に実施 |
| 9 | Step 3「CLAUDE.md 50行制限を維持」 | 現在 52 行（運用計画 §3 の目標は50行以内） | 棚卸しで 50 行以内へ戻しつつ二失敗ルールを明文化 |

---

## 1. 全体スケジュール

| 期間 | 作業 | 備考 |
|---|---|---|
| 8/8〜8/11（4連休） | Step 0 → 1 → 2（コードを触る重い作業） | 実装計画 v5 の開発期間内に収める。8/11 夜に消化状況を見てスコープカット判断 |
| 8/11〜8/12 | Step P: リポジトリ公開化 | Step 2（セキュリティレビュー）完了が前提条件 |
| 8/12〜8月中 | Step 3 → 4（ドキュメント突き合わせ・RUNBOOK） | 8/12 以降は運用期間（原則コード凍結）。ドキュメント系のみ |
| 8月末〜9月上旬 | Step 5 → 6（マニュアル改訂・v0.2.0 リリース） | キャプチャ撮影は手作業 |
| 9月〜10月上旬 | Step 7（開発記録・章単位ドラフト） | 技術書典21 の書籍素材。**第一期比較の定量データ収集は本ステップ前に前倒し** |
| 10月中旬 | Step 8（KPT＋第一期との比較＋第三期引き継ぎ） | 比較レポートは書籍の中核素材 |
| 10月下旬〜 | 予備期間 | 入稿作業と競合させない |

締切構造: 「セイレーン」入稿 11/5 ／ 技術書典21 入稿 11/9（イベント 11/23）

**運用期間ルールとの整合**: 8/12 以降は原則コードに手を入れない（実装計画 v5）。Step 1〜2 の修正が 8/11 までに終わらない場合は、残件を GitHub Issue に起票して「持ち越さずカット」を基本とする。カットできない重大セキュリティ修正のみ、運用期間の計画的例外として個別に判断する。

---

## 2. 各ステップ計画

### Step 0: 現状の凍結（半日）

**目的**: リファクタリング前の基準線を確定する。

- [x] `docs/TESTING.md` 新規作成: 全機能の回帰テストチェックリスト。**第一期の `docs/TESTING.md`（514行）を雛形として参照**しつつ、各 SPEC の E2E 節と manual.md の機能一覧から集約し、以後の各段階で使い回す
- [x] タグ付け: `v0.2.0-rc`（本リポジトリ初のタグ。main の HEAD へ）
- [x] TESTING.md に沿って回帰テストを一周、グリーン確認。NG 項目は GitHub Issue に起票し `/fix-issue` フローで個別修正

**注意**: 本番にはドッグフーディングの実データがある。回帰テストは検証専用プロジェクトを作成して実施し、終了時に削除して原状復帰する（破壊系操作は実データに触れない）。

**指示例**:
> 各SPECのE2E節とmanual.mdを素材に、全機能の回帰テストチェックリスト docs/TESTING.md を作成。作成後、チェックリストに従い回帰テストを一周し、結果をチェックリスト形式で報告。検証は専用プロジェクトを作って実施し終了時に削除。NG項目はIssue起票して別セッションで修正。

### Step 1: コードレビュー＆リファクタリング

**目的**: 保守性向上。レビューと修正を分離し、勝手な大改造を防ぐ。

- [x] レビュー（修正なし・分類リスト化のみ）。前回レビュー `docs/code-review-20260714.md` 以降の差分観点を含める
- [x] レビュー結果から対象を取捨選択し SPEC 化（人間が選ぶ）
- [x] 段階分割で実施（構造整理 → 重複統合 → 型・エラーハンドリング → パフォーマンス）
- [x] 各段階後に TESTING.md で回帰テスト

**ゲート**: 3ファイル以上にまたがるリファクタリングは着手前に人間へ設計確認（CLAUDE.md 既定）。認証・RLS・秘密情報に触れる場合は security-reviewer 必須。

**指示例（レビュー）**:
> 保守性向上を目的にコードベース全体をレビュー。修正はせず、問題点を「重複コード / 命名不統一 / 責務違反 / 型の緩さ / 未使用コード」に分類してリスト化し、影響範囲と優先度を付けて報告。docs/code-review-20260714.md の前回指摘の消化状況も併記。

### Step 2: セキュリティレビュー＋依存パッケージ更新

**目的**: コード確定後の状態で念入りに検証。公開化（Step P)の前提ゲートを兼ねる。

- [x] 全テーブルの RLS ポリシー網羅確認（マイグレーション**29本**）
- [x] API ルートの認証・認可チェック（**12本**＋proxy の保護範囲）
- [x] PAT・API キーがクライアントバンドル / ログに漏れる経路の確認（`lib/git/credentials.ts` の `GitCredentialProvider` 経由の徹底）
- [x] 入力バリデーション（Server Action **20ファイル・106関数**＋zod スキーマ全ファイル）
- [x] `npm audit`（**監査時22件 → 10件**。next 16.2.10→16.3.0 で前回積み残しの postcss も解消。残りは vivliostyle 系の上流待ち）＋依存パッケージ更新
- [x] 修正後に TESTING.md で回帰テスト（2026-08-12・変更影響範囲に絞って実施・NG 0件）

**結果**: Critical / High ゼロ、Medium 2件・Low 6件。記録は `docs/security-audit-20260812.md`。
PR [#185](https://github.com/Motoki-N/nekonote-v2/pull/185)（Low 4件の修正＋M-1 の受容明文化）と
[#186](https://github.com/Motoki-N/nekonote-v2/pull/186)（依存更新）をマージ済み。
**M-2（伏せ字規約の抵触）は Step P へ送る**（現ファイル差し替えと git 履歴の扱いがセットのため）。

**参考**: 前回監査 `docs/security-audit-20260714.md`。今回は「前回以降の差分＋全体再点検」として実施し、結果は日付つきで docs/ に残す。security-reviewer の通過を完了条件とする。

**指示例**:
> セキュリティ観点で全体レビュー。観点: (1)全テーブルのRLSポリシー網羅性 (2)APIルートの認証・認可 (3)PAT・APIキーのクライアント露出経路 (4)入力バリデーション (5)npm audit結果。docs/security-audit-20260714.md 以降の差分を明示。修正提案は重大度順にリスト化、修正は承認後に実施。

### Step P: リポジトリ公開化【指示書外・今回追加】

**目的**: プライベートリポジトリを public へ切り替える。準備作業は完了済みで、残りは切り替えと最終確認のみ。

- [x] 前提条件: Step 2（セキュリティレビュー）完了（2026-08-12）
- [x] 公開前最終チェック: git 全履歴と docs（特に dev-log.md・incident-log.md）を「公開すべきでない情報」（CLAUDE.md の規約: 秘密情報の実値・実メールアドレス・私的リポジトリ名・未公開作品の実タイトル・インフラ識別子）の観点で最終走査
  - **クリーン確認**: 全337コミットの diff を走査し、APIキー・トークン・JWT の**実値混入はゼロ**。`.env.local` は一度もコミットされていない（追跡対象は `.env.local.example` のみ）。Supabase プロジェクト ref の混入もなし
- [x] **既知の要対処（監査 M-2。セッション89で発見 → セッション93の Step 2 監査で全走査し確定）**: 未公開作品の実タイトルに由来する例示文字列が **3ファイル4箇所**に現存（UIコンポーネントの入力例・manual §8・**zod エラーメッセージ**。後者2つはクライアントバンドルにも乗る）。**→ 4箇所すべてをテンプレートの公開サンプル値へ差し替え済み**。加えて本走査で**インフラ識別子の実値1箇所（dev-log）が新たに見つかり**、伏せ字化した
  - 具体的な該当箇所・文字列・コミットは**公開リポジトリの文書には書かない**（対応関係を自ら晒すため）。実体はリポジトリ外で管理している。監査資料 `docs/security-audit-20260812.md` M-2 も同じ方針で索引を持たない
- [x] **git 履歴の扱いを判断 → 「許容」を決定**（履歴書き換え／許容／リポジトリ再作成の3択）。決め手は、author メールが全337コミットのメタデータにあり公開リポジトリでは通常公開される情報である以上、部分的な書き換えは成立せず全コミット書き換えになること。その代償（75 PR / 108 Issue のコミットリンク断絶、dev-log 内31箇所の SHA 参照の無効化、Step 7 の開発記録の追跡性低下）が、履歴 diff を掘って初めて見える情報を隠す利益を上回らないと判断した。判断材料の詳細は `docs/security-audit-20260812.md` M-2
  - 再発防止: 本リポジトリのローカル `user.email` を GitHub の noreply アドレスへ切り替え済み（今後のコミットのみ。既存履歴のメタデータは不変）
- [x] 監査で残した「未確認項目」6件のうち、**CLI で検証できる3件を消化**（本番DB実状態＝マイグレーション29/29適用・全24テーブルRLS有効／allowlistトリガーの本番存在＝有効／Vercel環境変数＝`NEXT_PUBLIC_` への秘密情報の誤登録なし・cron関連はProduction専用を維持）。**この過程でリマインドメールのキー名の誤登録を発見**（INC-1 復旧時のタイプミス。詳細と対処は `docs/security-audit-20260812.md`）
- [x] 残る未確認項目3件のダッシュボード確認（Supabase Auth設定・Google OAuth クライアント設定・AIプロバイダのスペンド上限）→ **Step 4 で決着**。一度きりの確認ではなく `docs/RUNBOOK.md` §8.2 の四半期点検項目として恒久化した
- [x] GitHub での可視性切り替え: **ユーザー自身が操作**（外部サービスのダッシュボード操作はユーザー実施の流儀に従う）。2026-08-12 に public 化完了
- [x] 公開後の動作確認: CI グリーン・Issue テンプレート2種健在・MIT ライセンス認識・Vercel 連携正常（本番エイリアスは最新デプロイを指す）・README にバッジなし
- [x] **公開で無料になった GitHub のセキュリティ機能を有効化**: Secret scanning / Push protection / Dependabot security updates。**Secret scanning の全履歴スキャンは検出ゼロ**で、手動走査の結果が独立に裏づけられた。Dependabot のオープン3件（high 1・medium 2）は**いずれも `@vivliostyle/vfm` の推移的依存**で、Step 2 で「上流待ち」と整理した範囲と一致（新規対応なし）

**Step P 完了**（2026-08-12）。この過程でリマインドメールのキー名誤登録という設定不具合を発見・解消した（詳細は `docs/security-audit-20260812.md`）。

### Step 3: ドキュメント確認・整備

**目的**: 実装との乖離解消（第一期 Problem への直接の是正）。

- [x] 実在ドキュメントを実装と突き合わせ: SPEC-\*.md（**28本**。計画時の26本＋SPEC-manuscript-history・SPEC-refactoring-step1）/ README.md / claude-code-operation-plan.md / 実装計画 / 要求仕様 / manual.md
- [x] 乖離箇所を分類して報告 → 承認後にドキュメント側を更新。結果は `docs/doc-audit-20260812.md`
- [x] CLAUDE.md 棚卸し: 53行 → **50行**。スキャフォールド時の残骸コメント削除＋検証コマンド節の圧縮（-4行）、**二失敗ルールを設計原則に明文化**（+1行）
- [x] 【判断事項②】スキーマ一覧資料（SCHEMA.md 相当）は**新設しない**（決定どおり）。代わりに実装計画 §8.1 へ「ER図は策定時Draft・現行の一次資料は migrations」と明記した
- [x] `docs/SPEC-index.md` を新設（SPEC 28本の索引＋SPEC不在で実装した8機能の記録先）

**結果**: 乖離 **60件**。**「実装が仕様から逸脱」は 0 件**で、すべて「実装が先に進み文書が追いついていない」方向だった。

**構造的な原因**: 運用計画 §2 が第一期 Problem「ドキュメントと実装の乖離」への対策に掲げた「SPEC へ進捗を書き戻す」は、実運用では **dev-log への書き戻しが代替**として機能していた（dev-log は全件正確で、今回の突き合わせもそれを索引に成立した）。SPEC への書き戻しは、冒頭に「改訂:」行を積む運用を採った SPEC でのみ続いていた。→ **Step 8 の KPT 素材**（運用計画 §7.2 に記録）。

**採用した方針**:

- SPEC は**時点記録として本文を据え置き**、冒頭の「改訂:」行＋本文の取り消し線で上書きを追跡する形に統一（第二期の「着手前に書く」という価値＝Step 8 観点C の中核を損なわないため）
- 実装計画・運用計画は本文据え置きで**後日談節**を追加。要求仕様は**追補節 §9** を追加（Step 8 の判定基準線を動かさないため）
- SPEC を策定せずに実装した8機能は**遡って SPEC を書かず、SPEC-index.md に索引**を作って記録先（Issue・dev-log セッション）を明示

**Step 5 への申し送り**: manual.md の乖離は19件。うち事実誤り3件（ノート記法の「非対応」・エディタの「ブランチ切替未対応」×2箇所）は本 Step で修正済み。**残り16件は構成変更を伴うため Step 5 の外部向け改訂へ送る**（`docs/doc-audit-20260812.md` §1.4 がそのまま作業リストになる）。特に **Zenn連携・目次ボード・構成テンプレート6種・青空文庫書き出し・EPUB・原稿の履歴/diff・選択範囲校正・レビュー履歴タブ・設定の6セクション化**が丸ごと未記載。

**Step 4 への申し送り**: `ENCRYPTION_KEY` の説明ズレ（README・`.env.local.example`・incident-log INC-1）は本 Step では触れず、計画どおり RUNBOOK 完成と同時に修正する（下記 Step 4 の「執筆時に直す既知の記述ズレ」）。

**指示例**:
> docs/配下の全SPECとREADME・運用計画を実装と突き合わせ、乖離箇所を「ドキュメントが古い / 実装が仕様から逸脱」に分類して報告。修正方針の承認後、ドキュメント側を更新。

### Step 4: 保守資料の作成（`docs/RUNBOOK.md`）

**目的**: 半年後の自分・第三者が運用できる状態にする。

- [x] API キー再発行手順（AIプロバイダ別: Anthropic / OpenAI / Google）→ RUNBOOK §3.2。加えて `ENCRYPTION_KEY`（§3.3）・Supabase 系（§3.4）・`CRON_SECRET`（§3.5）・`RESEND_API_KEY`（§3.6）も網羅
- [x] Fine-grained PAT の更新・失効時対応 → RUNBOOK §3.7（必要権限・差し替え手順・失効時の症状表つき）
- [x] Supabase バックアップ・リストア手順 → RUNBOOK §5
- [x] Vercel 環境変数の更新手順（Preview/Production スコープの罠を含む→incident-log 参照）→ RUNBOOK §4（削除前チェックリスト・削除後の全量確認）
- [x] 障害時の切り分けフロー → RUNBOOK §6（最初の3分／全機能停止／機能別／ログイン不可／サイレント障害／切り戻し）
- [x] ランニングコスト棚卸し: ペルソナ別 API 使用量・単価、Supabase / Vercel 無料枠の消費状況。データ源は実装済みの `ai_usage_logs`＋`ai_usage_summary(days)`（設定画面の直近30日テーブル）→ RUNBOOK §7（棚卸しコマンド＋2026-07-18〜08-12 の実測スナップショット）
- [x] 【追加】定期点検（月次・四半期）→ RUNBOOK §8。監査が残した未確認項目3件（Supabase Auth設定・Google OAuth・スペンド上限）を恒久的な点検項目として組み込んだ
- [x] 【追加】保守対応フロー（Issue駆動）→ RUNBOOK §9。Step 5 での manual.md §12 削除に先立って移設済み

**注意**: 公開リポジトリになった後の作成となるため、実値・実識別子は書かない（手順と参照先のみ）。

**キーローテーション手順の執筆時の注意（2026-08-06 事前調査）**:

- **共通原則は「新旧併存 → 切替 → 検証 → 旧失効」**。旧キーを先に失効させない。Vercel は環境変数をデプロイ時にスナップショットするため、値の更新だけでは反映されず**再デプロイが必須**（INC-1 で確認済みの仕様）
- **`ENCRYPTION_KEY` の暗号化対象は GitHub PAT のみ**（`user_settings.github_pat_ciphertext`。暗号化は `lib/actions/settings.ts`、復号は `lib/git/credentials.ts` に限定。ノート本文は暗号化しておらず pgcrypto 利用もなし）。したがって現状のローテーションは再暗号化スクリプト不要で「新32バイト鍵に差し替え → 設定画面で PAT 再登録」で完結する。ただし**再発行不能な唯一の鍵**である点は変わらない。将来暗号化対象を増やしたら「差し替えるだけ」は成立しなくなる（旧鍵復号→新鍵再暗号化の移行スクリプトが必須になる）ことを RUNBOOK に明記する
- **`CRON_SECRET` は外部整合が不要**（Vercel Cron は環境変数の値をそのまま Bearer で送る）。新しい乱数への差し替えと再デプロイだけで完結する
- **Supabase キーの失効は方式依存**: 旧来の JWT ベースのキー（`eyJ...`）は失効が JWT secret ローテーションを伴い全セッションが切れる。新方式（publishable / secret key）なら個別ローテーション可。手順執筆前に Dashboard の API Keys 画面で現行方式を確認する
- **キー値を出力しない作法を手順に織り込む**: 存在確認は `grep -c '^KEY='` 形式（dev-log セッション記録の教訓）、疎通確認の curl は値を直接タイプせず環境変数経由で渡す
- **執筆時に直す既知の記述ズレ**（RUNBOOK 完成と同時に修正する）:
  - `.env.local.example` の `ENCRYPTION_KEY` コメント「ノート等の暗号化キー」→ 実態は GitHub PAT のみ
  - README の環境変数表の同項目も同様
  - incident-log INC-1 の重大化シナリオ「暗号化済みデータ（ノート・PAT）」→ 実際の影響は PAT 再登録に留まる（記録の改変ではなく追記形式で訂正する）

**Step 4 完了**（2026-08-12）。`docs/RUNBOOK.md` を新設（全9節）。README のドキュメント一覧にも追加した。

- **執筆時に直した記述ズレ 3件**: `.env.local.example` と README の `ENCRYPTION_KEY` 説明（「ノート等の暗号化キー」→ GitHub PAT のみ・再発行不能である旨を明記）、incident-log INC-1 の重大化シナリオ（元記述は残し、追記形式で訂正）。これで doc-audit の D-9 は解消
- **執筆中に判明したリスク**: 本番 Supabase は **PITR 無効・物理バックアップ0件**（`supabase backups list` で確認）。プラットフォーム側に復元手段がないため、**自前の `db dump` が唯一の復旧経路**である。RUNBOOK §5.1 に警告として明記し、§8.1 の月次点検に「自前バックアップの取得」を入れた。有償プランへの移行検討はユーザー判断に委ねる
- **監査の未確認項目3件の決着**: Dashboard でしか確認できない3件（Supabase Auth設定・Google OAuth クライアント設定・AIプロバイダのスペンド上限）は、一度きりの確認ではなく **RUNBOOK §8.2 の四半期点検項目として恒久化**した。運用期間に入る本プロジェクトでは、こちらの形の方が実効性がある
- **コスト実測（2026-07-18〜08-12・26日間）**: AI 呼び出し 94回・入力 502,755 tok・出力 110,862 tok。Anthropic 分の概算は約 $2.0（リスト価格換算）で、**月額数ドル規模**。DB 14MB・Storage 約9MB で無料枠には十分な余裕があり、先に効く制約は容量ではなくバックアップの不在だった
- **Step 5 への申し送り**: manual.md §12（保守対応フロー）の内容は **RUNBOOK §9 へ移設済み**。Step 5 の外部向け改訂では manual.md から §12 を削除し、「不具合を見つけたら Issue へ」の導線だけ残せばよい

### Step 5: 外部向けユーザーマニュアル改訂

**目的**: 第三者に「ネコノテAIとは何か」を説明する。開発・保守事項は含めない。

- [x] 【判断事項③】既存 `docs/manual.md`（529行）を外部向けへ改訂する。推奨: 別ファイルは作らず manual.md 自体を外部向けに改訂し、§12 保守対応の内部向け記述は Step 4 の RUNBOOK へ移設
- [ ] 撮影用デモデータの用意。推奨: シード機構は新設せず、見栄えのする架空プロジェクト1件を**手作業で**作成（設計原則「最もシンプルな方法」に従う。未公開作品の実データは撮影に使わない）
- [x] 構成は利用フロー順（構想→企画→設計→執筆→校正）を維持。冒頭に手帳→プロットモデルの思想説明を追記
- [x] キャプチャ挿入位置を `[SCREENSHOT: 説明]` プレースホルダで指定（現状 0 件）→ **17箇所**
- [ ] キャプチャ撮影・差し込みは手作業

**指示例**:
> docs/manual.md を外部ユーザー向けに改訂。冒頭に手帳→プロットモデルのコンセプト説明を追加し、保守対応の内部向け記述はRUNBOOKへ移設。キャプチャ挿入位置は [SCREENSHOT: 対象画面の説明] 形式で指定。

**改訂完了**（2026-08-12）。529行 → 657行。残るのは撮影作業（デモデータ作成・キャプチャ・差し込み）のみ。

- **構成**: §12「保守対応」を削除（RUNBOOK §9 へ移設済み）し、その枠に **§11「Zenn連携」**（完全未記載だった機能）を新設。全14章・利用フロー順は維持。§6 は執筆ジャンルで画面が変わる実状に合わせて「構成設計（ビートボード / 目次ボード）」へ改題
- **§1 を「ネコノテAIとは」へ改題**し、§1.1 に「手帳→プロットモデル」の思想説明を追加（入力は白紙から / 分類は後から / 落とし込みはテンプレート挿入 / 落とし込み後も自由 → 帰結として固定の入力フォームを持たない、企画書＝表紙・ノート＝本文）
- **doc-audit §1.4 の残16件（A-22〜A-29・A-31・A-34〜A-40）を全解消**。加えて突き合わせで拾い切れていなかった **3件**も直した:
  - **企画書のバージョン履歴・差分**（doc-audit A-2 は SPEC 側の指摘だったが manual にも未記載だった）
  - **シーンレビューの合否判定**「なし」→ 実装は判定バッジ＋「シーンを通す」承認ゲートあり（`beat-board.tsx`）。TESTING.md §250 が「Step 3 で解消」としていたが未消化のまま残っていた事実誤り
  - **ノートの添付ファイル**（10MB・png/jpg/webp/gif/pdf/txt）の記載
- **外部向けへの整理**: Vercel 環境変数への言及を除去（「APIキーはVercelの環境変数で管理」等）、§13 末尾の報告導線は「Issue テンプレートの選び方・1 Issue = 1件・書くべき情報」だけを残して修正フロー（`/fix-issue`・設計確認の5条件・PRマージ）は RUNBOOK へ委ねた
- **§14 の訂正**: 「執筆画面にAI機能を一切載せていない」は選択範囲校正（SPEC-proofread-selection）の実装後は不正確なため、「校正以外のAI機能を載せていない」＋機能別表に Zenn 行を追加する形へ改めた

**撮影の申し送り**（残作業）:

- 撮影用デモデータは**架空作品1件**を手作業で用意する。必要なのは「ノート数点＋仮タイトルタグ」「企画書（4フィールド記入済み・レビュー1往復）」「ビートボードにシーン10枚前後＋転換点＋感情の起伏」「原稿リポジトリ（テンプレート初期セットアップで作成し、章2〜3本）」「アトリエの生成画像数点」。**未公開作品の実データは使わない**（公開リポジトリ規約）
- プレースホルダ17箇所の内訳: ダッシュボード2 / ログイン1 / ノート2 / プロジェクト・企画書3 / ボード2 / 原稿1 / 縦書きエディタ2 / アトリエ1 / 相談1 / Zenn1 / 設定1
- 画像は `docs/images/` に置き、`![説明](images/xxx.png)` で差し替える想定（撮影時に判断）

### Step 6: リリース確定

- [x] `CHANGELOG.md` **新規作成**（本リポジトリ初。以後のリリースで更新していく起点）
- [x] package.json のバージョンを 0.1.0 → 0.2.0 へ更新
- [x] `v0.2.0` タグ付け（注釈付きタグ。main の `17941c5` へ。本リポジトリ2つ目のタグ——1つ目は Step 0 の `v0.2.0-rc`）
- [x] GitHub Release 作成（Step P 完了済みのため公開状態で見える）→ [v0.2.0](https://github.com/Motoki-N/nekonote-v2/releases/tag/v0.2.0)

**Step 6 完了**（2026-08-12）。PR [#191](https://github.com/Motoki-N/nekonote-v2/pull/191) をマージ後、タグと Release を main の HEAD に対して作成した。

- **Release ノートは CHANGELOG の v0.2.0 節をそのまま流用**し、冒頭にマニュアル・RUNBOOK・SPEC索引への案内を足した。
  ドキュメントへの相対リンクは **`blob/v0.2.0` 固定の絶対URLへ変換**している（Release ページはリポジトリのルート基準で解決されるため相対リンクでも表示はできるが、
  後の改訂で内容がズレないよう**リリース時点のスナップショットへ固定**した）

**CHANGELOG の方針**（2026-08-12）: Keep a Changelog 準拠・日本語。v0.2.0 は**第二期リポジトリの初回リリース**として、
第一期からの3転換（手帳→プロットモデル / AIのペルソナ化 / 原稿のGit移管）＋機能を11領域に分けて列挙する構成にした。
コミット単位ではなく**機能単位**でまとめ、詳細は manual.md・SPEC-index.md・dev-log へリンクする
（349コミット・79PR を羅列しても読めないため）。第一期の問題点6件が解消されたかの**判定は書かない**——
それは Step 8 観点A の成果物であり、リリースノートで先取りすると Step 8 の判定基準が動くため。

- `package-lock.json` は `npm install --package-lock-only` だと無関係な bundled optional 依存
  （`@tailwindcss/oxide-wasm32-wasi` 配下）まで書き足されたため revert し、**バージョン行2箇所のみ**を直接編集した
  （運用期間の「原則コード凍結」に沿ってリリース差分をノイズなしに保つ）

### Step 7: 開発記録の作成

**目的**: 技術書典21向け書籍の素材。**時系列日記ではなく章単位ドラフト**で作成。

- [x] 素材: git log / **docs/dev-log.md（98セッション分）** / **docs/incident-log.md** / 各 SPEC-\*.md / claude-code-operation-plan.md §2（第一期KPT→第二期対応表）
- [x] 書籍の章候補単位で Zenn 記事ドラフト化 → **章立て10本＋終章を `docs/dev-story/README.md` に確定**
- [x] 各章に第一期の失敗を対比として記載（失敗→是正の構造。対応表 §2 が索引になる）→ 章立ての各項目に「第一期の対比」「効果測定」を明記
- [x] Step 8 の「第一期との比較」の定量データを本ステップの前に収集しておく → **`docs/第一期・第二期比較.md` を新設**（定量データのみ。観点A/B/C の判定は Step 8）
- [ ] 章ドラフトの執筆（1セッション＝1章）— **第9章・終章 完了 / 残り9章**（終章は Step 8 完了により前倒しで執筆）

**Step 7 の1セッション目**（2026-08-12）: 定量データ収集 → 章立て確定 → 第9章ドラフト。

**第一期の再計測で、これまでの記録の誤りが3件見つかった**（いずれも比較資料とメモに訂正済み）:

1. **ベースラインの計測点**: 計画書 §2 Step 8 の表（41コミット / 130ファイル / 23,495行）は 8/7 に **HEAD** で測った値で、
   v0.1.0 以降の保守コミット8件（2026年の環境復旧・CVE対応を含む）を含んでいた。**v0.1.0 時点の正は 37コミット / 118ファイル / 22,421行**。
   計画書の表は時点記録として修正せず、`docs/第一期・第二期比較.md` の値を正とする
2. **「第一期のドキュメントは完成後に書いた」は事実誤り**。実際は**開発10日間の5日目**（2025-06-30）に
   6本・3,169行が**1コミットで一括生成**され、以後 v0.1.0 までに docs へ触れたコミットは2件のみ。
   → 「完成後に書いたから乖離した」という説明は成立しない。**Step 8 観点C の論点が変わる**
3. **「第一期はマイグレーション0本」は不正確**。`database/` に手書きSQL 9本があった。無かったのは
   **バージョン管理された適用機構**（`supabase/` ディレクトリ自体がない＝SQL Editor へ手貼り）

**指示例**:
> git log、docs/dev-log.md、docs/incident-log.md、各SPEC、運用計画§2の第一期KPT対応表を読み、第二期開発の記録を作成。形式は時系列ではなく書籍の章候補単位でZenn記事ドラフトとして出力。各章に対応する第一期の失敗も対比として記載。

### Step 8: 振り返り（KPT）＋第一期との比較＋第三期引き継ぎ

**目的**: 第三者開発に向けた KPT と、第三期キックオフの入力を作る。あわせて第一期との比較を成果物として残す。

- [x] KPT 作成。第一期 KPT の Try 項目（運用計画 §2 の対応表）が第二期で機能したかの検証を含める（書籍の「是正の効果測定」に転用）→ 比較レポート §6（**第一期 Problem 6件への対策のうち5件が機能・1件が不発**）
- [x] **第一期との比較レポート作成**（→ 下記「比較の3観点」。成果物: `docs/第一期・第二期比較.md`）
  - [x] 定量データ（§1〜§2。Step 7 で前倒し収集）
  - [x] **観点A（機能）・観点B（性能・品質）**（2026-08-12・Step 8 の1セッション目）
  - [x] 観点C（開発の進め方・書籍の中核）
**Step 8 完了**（2026-08-12・2セッション）。成果物は `docs/第一期・第二期比較.md`（観点A/B/C＋KPT）と `docs/第三期引き継ぎメモ.md`。
**これで締めくくり作業 Step 0〜8 のうち、残るのは Step 5 のキャプチャ撮影と Step 7 の章ドラフト9本＋終章のみ**（どちらも継続作業）。

- [x] 第三期引き継ぎメモ: シリーズ機能の**確定済み設計方針**を1枚に整理 → `docs/第三期引き継ぎメモ.md`。現状は dev-log 記載のインタビュー確定6項目（series テーブル新設＋projects.series_id / series_proposals 新テーブル / 1リポ1シリーズ・巻サブディレクトリ / 伏線・年表の最小構造化 / キャラ巻状態はノート方式 / 階層要約はオンデマンド）のみで、**スキーマ・コードへの実装は未着手**。この「設計確定・実装ゼロ」の状態を正確に引き継ぐ

#### 比較の3観点

**観点A: アプリの機能の比較**

- [x] 要求仕様 §2 の「第一期の問題点」6件が実際に解消されたかを1件ずつ判定（①認証切れ ②ノートの自由度・Markdown非対応 ③AIが校正限定 ④原稿のDB管理 ⑤構成の見えづらさ ⑥テーマ対応の断念）。**解消 / 部分解消 / 未解消**の3段階で、根拠となる実装箇所とあわせて記録する
- [x] 要求仕様 §1.3 の設計思想3転換（手帳→プロットモデル / AIのペルソナ化 / 原稿のGit移管）が実装として成立したかの評価
- [x] 第二期で新規追加された機能の棚卸し（縦書きエディタ・入稿PDFビルド・アトリエ・対話型ペルソナ・Zenn連携・AI使用量計測など、当初要求になかったもの）
- [x] **第一期にあって第二期で意図的に捨てたもの**の記録と、その判断が正しかったかの評価（`characters` / `chapters` 等の構造化テーブル → ノート＋テンプレート方式へ。実装計画 §3 の方針）

**観点A 完了**（2026-08-12）: 比較レポート §3。**第一期の問題点6件はすべて解消**と判定。
判定の強さに差があり、#2〜#6 は実装の有無で確認できるが **#1（認証切れ）は「再発しなかったか」でしか確認できない**ため §3.1.1 で個別に扱った——
Issue 108件中に該当起票ゼロ、かつ**第一期側は v0.1.0 の1年後（2026-08-08）に `auth-lock.ts`（Web Locks 固着の回避策）を追加している**ことを確認。
第一期の「切れる」問題はクライアント側 `getSession()` 依存の構造に由来しており、第二期は proxy によるサーバー側更新でこの経路自体を持たない。
「読み込み専用」からの逸脱（§3.4）は**転換の徹底として評価**し、逸脱としない（保管場所の方針は一度も揺らいでいない）。

**観点B: 性能・品質の比較**

- [x] 定量比較が可能なもの: コード規模、マイグレーション管理の有無、AI使用量・コスト（第二期は `ai_usage_logs` に実測あり）
- [x] **第一期には計測データが存在しない**ため、応答速度等の直接比較は不可。第一期側は要求仕様 §2 に記録された体感問題（認証切れの頻発等）を基準とし、「同じ問題が第二期で再発しなかったか」の形で検証する
- [x] 品質面の比較材料: 第二期は security-reviewer ゲート通過記録・incident-log（INC採番）・typecheck/lint の hook 強制あり。第一期は同等の記録が無いこと自体を差分として記録

**観点B 完了**（2026-08-12）: 比較レポート §4。**規模が3倍（マイグレーション9→29本・Route Handler 3→12本・Server Action 8→20ファイル）になっても Critical / High はゼロのまま**で、
1回目の監査で確立した規律が全件で維持されていたことを2回の監査記録から確認した（これが第二期で唯一「実測で答えられる」品質指標）。
**第一期は RLS を全テーブルで明示的に無効化していた**（`-- RLS無効化（個人利用のため）`）ことが判明し、§4.2.1 として独立させた——
「まだ書いていない」ではなく意識的に外していた。第二期が同じ個人利用の前提で全24テーブルを有効にしたのは、
**利用者ではなく書き手（AIエージェント）が増えたから**という整理を記録した。

**観点C: 開発の進め方の比較**（書籍の中核）

**観点C 完了**（2026-08-12）: 比較レポート §5。**転換は機能した**が、効き方が想定と違った項目が2つある——
①常駐コンテキストは「減らしたから」ではなく**性質で仕分けて減らせる形にしたから**機能した（全部が助言だった状態を、hooks=強制／skills=知識／subagents=検証／SPEC=設計に分けた）
②**「機能ごとに実装の直前に書く」ことの効果は、文書を最新に保つことではなく、実装が仕様から外れるのを防ぐことに出ていた**（乖離60件の全件が「実装が先・文書が後追い」で、仕様からの逸脱は0件）。
手戻りの比較では、第一期 Day16 相当の事象は**起きなかった**が、より正確には**問題の受け止め方が違った**——
第一期は開発7日目に「不具合修正 Part1〜3」を**まとめてコミット**し、第二期は開発6日目に Issue を**40件まとめて起票**して1件ずつ直列消化、17件は運用期間へ持ち越した（＝カット可能な状態で保持）。

- [x] Claude Code の使い方の転換を実データで対比: **指示書型 CLAUDE.md（第一期 `.claude/old.md` 5,173行）→ 分業型（第二期 CLAUDE.md 52行＋SPEC 26本＋skills 4＋subagents＋hooks）**。運用計画 §1「第一期からの転換点」が主張どおりに機能したかを検証する
- [x] ドキュメントの作り方の対比: 第一期は**完成後に書いた網羅ドキュメント7本（4,357行）**、第二期は**着手前に書く SPEC 26本**。「ドキュメントと実装の乖離」（第一期 Problem）の構造的原因が解消されたかを論じる
- [x] スキーマ管理の対比: 第一期はマイグレーション0本（DB直接変更）→ 第二期は28本＋プランモード承認＋直接変更禁止
- [x] 開発リズムの対比: 第一期41コミット vs 第二期281コミット・80セッション。1機能=1セッション規律・二失敗ルール・Issue駆動フロー（`/fix-issue`）の効果
- [x] 手戻りの比較: 第一期の「Day16に14項目の大量修正」（運用計画 §2）に相当する事象が第二期に起きたかを dev-log / incident-log から検証

#### 実測済みベースライン（2026-08-07 時点）

比較レポート作成時の出発点。数値は作業時に再取得して更新する。

| 指標 | 第一期（v0.1.0） | 第二期（現在） |
|---|---|---|
| コミット数 | 41 | 281 |
| TS/TSX ファイル数 | 130 | 190 |
| TS/TSX 行数 | 23,495 | 32,018 |
| CLAUDE.md（常駐コンテキスト） | 5,173行（`.claude/old.md` の指示書型） | 52行（分業型） |
| 事前仕様（SPEC） | 0本 | 26本 |
| 事後ドキュメント | 7本・4,357行 | —（SPEC へ前倒し） |
| マイグレーション | 0本（DB直接変更） | 28本 |
| 開発期間 | 2025-06-26 〜（v0.1.0） | 2026-07-12 〜 |
| セッション記録 | なし | dev-log 80セッション＋incident-log |

**注記**: 第一期リポジトリは非公開のまま。本比較レポートは公開リポジトリに置くため、リポジトリ名・実データ・秘密情報は伏せ字規約に従い、**数値と構造の対比のみ**を記載する。

**着手タイミングの補足**: 本比較は Step 7（開発記録）の章立ての骨格そのものになる。運用計画 §5 が「第一期→第二期の比較データを意識的に残す」と定めた回収地点であり、**定量データの収集だけは Step 7 着手前に前倒ししてよい**（Step 8 の他項目は予定どおり10月中旬）。

---

## 3. ユーザー判断事項（2026-08-05 全4点承認済み・推奨案どおり決定）

| # | 事項 | 決定 |
|---|---|---|
| ① | 公開化のタイミング | Step 2（セキュリティレビュー）完了直後（8/11〜8/12）。Step 6 の Release 前であること |
| ② | SCHEMA.md 相当の新設要否（Step 3） | 新設しない。migrations を一次資料とし、RUNBOOK に必要最小限のみ記載 |
| ③ | manual.md の扱い（Step 5） | 別ファイル化せず外部向けに改訂。保守記述は RUNBOOK へ移設 |
| ④ | バージョン番号 | 指示書どおり v0.2.0（初タグ。v0.1.0 は遡って打たない）。package.json も 0.2.0 へ同期 |

---

## 4. リスク・注意

- **運用期間との整合**: Step 1〜2 の積み残しは 8/12 以降へ持ち越さない（Issue 起票してカットが基本）
- **本番実データの保護**: 回帰テスト・撮影は検証/デモ専用プロジェクトで行い、実原稿・実ノートに触れない
- **公開リポジトリ規約**: Step P 以降に作る全成果物（RUNBOOK・CHANGELOG・Release ノート・dev-log 追記）に伏せ字規約を適用する。Step P 以前の成果物も公開時に露出するため同基準で書く
- **キャパシティ**: 9月以降は「セイレーン」執筆・入稿と並走する。Step 7〜8 は1回のセッションを小さく刻む（章1本単位など）
