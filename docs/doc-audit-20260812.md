# ドキュメント突き合わせ結果（2026-08-12・締めくくり作業 Step 3）

- 対象: SPEC-\*.md **28本** / README.md / CLAUDE.md / claude-code-operation-plan.md / 実装計画 / 要求仕様 / manual.md
- 方法: 各ドキュメントを通読し、記載内容を実装（コード・マイグレーション29本・`.claude/` 構成）と照合。パス表記は機械的に実在確認
- 分類: **A ドキュメントが古い**（実装が正・文書を直す） / **B 実装が仕様から逸脱**（仕様が正・実装を直す） / **C SPEC不在**（実装済みだが仕様書がない） / **D 記述ミス**（誤記・パス誤り） / **E 方針判断**（人間の決定が必要）

## 0. 総評

- **B（実装が仕様から逸脱）は 0 件**。仕様に反する実装は見つからなかった。乖離はすべて「実装が先に進み、文書が追いついていない」方向
- 乖離の**構造的な原因は一つ**に集約される——SPEC への書き戻しが**改訂行のある SPEC でしか行われていない**。運用計画 §2 は第一期 Problem「ドキュメントと実装の乖離」への対策として「チェックポイントごとに SPEC へ進捗を書き戻す」を掲げたが、実運用では **dev-log への書き戻しが代替**として機能した。dev-log は全件正確で、今回の突き合わせもほぼ dev-log を索引に成立した
- 影響度が高いのは **manual.md（外部向け・Step 5 で改訂予定）と 要求仕様（Step 8 の比較の基準）**。SPEC 群の乖離は「後続SPECが上書きした事実が本文に書かれていない」型が中心で、実害は「単体で読むと古い仕様に見える」こと
- **計画書の前提の訂正**: 計画書は「SPEC 26本」としているが、その後 SPEC-manuscript-history（8/9）・SPEC-refactoring-step1（8/12）が追加され現在 **28本**

---

## 1. A: ドキュメントが古い（実装が正）

### 1.1 SPEC のスコープ外項目が実装済みのまま残っている（優先度 高）

「§7 スコープ外」に書かれたまま実装された機能。SPEC 単体で読むと未実装に見える。

| # | ドキュメント | 記述 | 実状 |
|---|---|---|---|
| A-1 | SPEC-notes §7 | 「ノートのバージョン履歴・エクスポート」をスコープ外 | 両方とも実装済み（`20260723000004_note_versions.sql`＋`components/notes/note-history-panel.tsx` / `app/api/notes/export/route.ts`・Issue #38） |
| A-2 | SPEC-proposal-review §7 | 「企画書の版管理・差分表示」をスコープ外 | 実装済み（`20260809000001_proposal_versions.sql`・PR #168・Issue #64） |
| A-3 | SPEC-vertical-editor §7 | 「EPUB等の電子書籍出力」をスコープ外 | 実装済み（Issue #119・PR #120。`v*-epub` タグ＋`build-epub.yml`＋`book.config.epub.js`＋`theme-epub.css`） |
| A-4 | 要求仕様 §6 | 「表紙・挿絵の生成機能」をスコープ外 | 実装済み（SPEC-illustrator・アトリエ） |
| A-5 | 要求仕様 §6 | 「AIによる要約・あらすじ作成機能」「AIによるキャッチコピー作成機能」をスコープ外 | 両方とも実装済み（あらすじ作成 …1008 / キャッチコピー作成 …1009・`20260717000001`） |

### 1.2 後続SPECによる上書きが本文に反映されていない（優先度 中）

SPEC-beat-board は冒頭に「改訂:」行を積んで追跡できているが、以下は上書きされた側に注記がない。

| # | ドキュメント | 記述 | 上書きした側 |
|---|---|---|---|
| A-6 | SPEC-conversational-personas §2・§3.1・§4.1 | 「ペルソナごとに1スレッドを継続」「スレッド一覧UIは作らない」「部分ユニークインデックス」 | SPEC-chat-thread-list が複数スレッド化＋`/chats` 新設＋インデックス撤去（`20260714000003`）。chat-thread-list §1 側には引き継ぎ記載あり |
| A-7 | SPEC-conversational-personas §3.1・§5.1 | 「会話をリセット」ボタン・`resetDeepDiveThread` の共用 | 「新しい会話」へ置換。実関数は `deleteThread`（`lib/actions/chat.ts`） |
| A-8 | SPEC-vertical-editor §5・§8 論点1 | 配置は「`app/(app)/editor/` 配下」 | phase2 論点B で `/projects/[id]/editor` に確定（実装も同じ）。親SPEC が旧決定のまま |
| A-9 | SPEC-vertical-editor-phase2 §11・§12 論点E | 「デフォルトブランチのみ。切替・作成UIは後続フェーズ」 | phase5 で解除済み（本文に phase5 への参照はあるが、§11 スコープ外の取り消し表記がない） |
| A-10 | SPEC-proofreading §7 | 「ブランチ選択・コミット履歴表示・差分ビュー」「選択範囲の校正」をスコープ外 | 履歴・diff は SPEC-manuscript-history、選択範囲校正は SPEC-proofread-selection で実装済み（各子SPECの §5 親SPEC欄には記載あり／親側に取り消し表記がない） |
| A-11 | SPEC-notes §3.1 | 一覧の「並び順: 更新日時の降順」（固定） | ソート切替UIあり（`components/notes/sort-menu.tsx` / `sort-options.ts`） |
| A-12 | SPEC-notes §3.2・§7 | 「表・画像・コードブロック等の高度な記法、ファイル添付」の取り消し線＋Issue #35 昇格は反映済み | ※ここは正しく更新されている（good example） |
| A-13 | SPEC-refactoring-step1 段階4 | 完了条件「vertical-editor.tsx が目安 800 行以下」 | 実績 1,044 行で停止（PR #184 に理由を明記して合意済み）。SPEC 側に結果が未記載 |

### 1.3 要求仕様が現状を映していない（優先度 高・Step 8 の基準になるため）

| # | 箇所 | 記述 | 実状 |
|---|---|---|---|
| A-14 | §3.5 ペルソナ表 | 標準ペルソナ **6人** | **9人**（＋イラストレーター・技術書編集者・エンジニア読者。`lib/ai/personas.ts`） |
| A-15 | §3.5 データ構造 | `persona_type` は「reviewer / conversational」の2値 | **3値**（＋`illustrator`。`20260717000002`） |
| A-16 | §3.5 標準テンプレート | プロファイル **5種**（企画書・キャラクター・構成・シーン・校正） | **13種**（＋読者講評・売り込み分析・あらすじ・キャッチコピー・技術書4種） |
| A-17 | §3.5 `review_profiles.target_phase` | 「企画 / キャラクター / 構成 / シーン / 校正」 | ＋`manuscript`（講評。`20260714000001`） |
| A-18 | §4.4 初期マッピング | 能力レベルは high / medium / low の3帯 | ＋`image` 帯（Google `gemini-3.1-flash-image-preview`。`DEFAULT_MODEL_MAP`） |
| A-19 | §1.1・全体 | 目的が「小説」に限定 | **汎用執筆支援（技術書）へ拡張済み**（`proposals.writing_genre` の novel/tech_book/other・目次ボード・技術書ペルソナ）。SPEC-genre 系3本が実在するのに要求仕様に反映がない |
| A-20 | §5 技術スタック | Zenn 連携の記載なし | Zenn 記事投稿機能あり（SPEC-zenn-integration・`/zenn/new`） |
| A-21 | §3.4 | 「ネコノテはGitHub連携により原稿を**読み込み専用**で取得する」 | 書き込み経路が多数（校正コミット・保留書き戻し・エディタ保存＝コミット・画像/設定/タグ/ブランチ/PR・Zenn）。§3.4 の 2026-07-16 追記で一部は補われているが、この一文が残っている |

### 1.4 manual.md（外部向け・Step 5 で改訂予定・優先度 高）

Step 5 の改訂作業の入力としてそのまま使える形で列挙する。

| # | 箇所 | 記述 | 実状 |
|---|---|---|---|
| A-22 | §1 | 「標準ペルソナ**7人**」「レビュープロファイル**9種**」 | 9人 / 13種 |
| A-23 | §5 | 「プロジェクト配下は 企画書 / ビートボード / 原稿 / エディタ の**4タブ**」 | **5タブ**（＋レビュー履歴。`components/projects/project-tabs.tsx`） |
| A-24 | §5 | 企画書エディタは「ジャンル / ターゲット層の**2フィールド**」 | **4フィールド**（執筆ジャンル・執筆目的・内容ジャンル・ターゲット層。SPEC-genre） |
| A-25 | §5 | プロジェクト作成に執筆ジャンル選択の記載なし | 作成ダイアログに select あり（既定=小説） |
| A-26 | §6 | ビートボードは4部構成のみ | **6テンプレート制**（SPEC-structure-templates）＋非小説ジャンルは**目次ボード**（SPEC-outline-board）。どちらも未記載 |
| A-27 | §6 | 編集ダイアログの記載が旧仕様 | 4観点は placeholder ではなく**新規作成時の初期値**（#155）／**保存ボタン廃止・閉じると保存**（#169）／**前後シーン移動ボタン**（#171）／**ノート紐づけ・原稿ファイル紐づけ**（#56）が未記載 |
| A-28 | §6 | 構成レビューの「ノートに転記」未記載 | 実装済み（#173） |
| A-29 | §4 | ノート記法は「見出し・リスト・太字/斜体・リンク・引用・区切り線」 | ＋**表・画像・コードブロック・ファイル添付**（Issue #35） |
| A-30 | §13 既知の制限 | 「ノートの記法: 表・画像・コードブロックは**非対応**」 | **誤り**（実装済み）。§4 と合わせて要修正 |
| A-31 | §4 | ノートのバージョン履歴・一括エクスポート・ソート切替が未記載 | いずれも実装済み（A-1・A-11） |
| A-32 | §8 | 「対象は**デフォルトブランチのみ**」 | ブランチ切替・作成・PR作成に対応（phase5） |
| A-33 | §13 既知の制限 | 縦書きエディタ「デフォルトブランチのみ（ブランチ切替・PR連携は**未対応**）」 | **誤り**（A-32 と同件） |
| A-34 | §8 | 青空文庫／カクヨム／なろう形式の**書き出し**が未記載 | 実装済み（SPEC-aozora-export） |
| A-35 | §8 | 入稿ビルドが PDF のみ | **EPUB ビルド**（`v*-epub`）も選択可（A-3） |
| A-36 | §7 | 原稿タブの**コミット履歴・diffビュー**が未記載 | 実装済み（SPEC-manuscript-history） |
| A-37 | §8 | 縦書きエディタの**選択範囲校正**が未記載 | 実装済み（SPEC-proofread-selection） |
| A-38 | §11 | 設定は「**4セクション**」 | **6セクション**（AIモデル設定 / **AI使用量（直近30日）** / ペルソナ / レビュープロファイル / GitHub連携 / **Zenn連携**） |
| A-39 | 全体 | **Zenn 連携機能（`/zenn/new`・記事投稿・画像アップロード）が完全に未記載** | 実装済み（SPEC-zenn-integration） |
| A-40 | §3 | マイルストーン**締切リマインドメール**が未記載 | 実装済み（`/api/cron/milestone-reminders`・Issue #51） |
| A-41 | §13 レートリミット | 記載の各値 | 実装と一致（chat 10/分・300/日、review・proofread 各 3/分・60/日、案出し 5/50、画像生成 2/20）。**要修正なし** |

### 1.5 運用計画・実装計画（優先度 中〜低・時点記録の性格が強い）

| # | 箇所 | 記述 | 実状 |
|---|---|---|---|
| A-42 | 運用計画 §6「直近アクション」 | 7/18 改訂のまま（「〜8/11 ドッグフーディング」「8/12〜 運用期間」） | 8/12 到達済み。締めくくり作業（Step 0〜P 完了・Step 3 実施中）が未反映 |
| A-43 | 実装計画 §3 ER図 | `personas.persona_type "review\|dialogue"` / `review_profiles.target_phase` に manuscript なし / `scenes.part` は4値 / `projects` に schedule・structure_template・writing_genre 等なし | いずれも拡張済み（A-15〜A-17 と同根。ER図は 7/11 の Draft） |
| A-44 | 実装計画 §5 Sprint 0 | 「DBスキーマ全体（**16テーブル**＋RLS）」 | 現在 **24テーブル**（Step P の本番確認で確定） |
| A-45 | 実装計画 §4 画面一覧 | 11画面 | アトリエ / Zenn / 縦書きエディタ / レビュー履歴 / `/chats` が未掲載 |

---

## 2. C: SPEC が存在しない実装済み機能（優先度 中）

CLAUDE.md の「仕様が存在しない機能は、先に仕様策定を提案する」に対し、以下は SPEC を作らずに実装された（設計判断は dev-log と Issue に残っている）。**遡って SPEC を書くかは判断事項（E-3）**。

| # | 機能 | 記録の所在 |
|---|---|---|
| C-1 | **EPUB 出力**（`v*-epub` タグ・build-epub.yml・EPUB専用テーマ） | dev-log セッション64（設計3点を AskUserQuestion で確定）・Issue #119 |
| C-2 | **横書き対応**（テーマ追従の自動判定） | dev-log セッション61・Issue #97 |
| C-3 | **ノートの版管理・企画書の版管理**（note_versions / proposal_versions・差分表示） | dev-log セッション83・Issue #111 / #64。マイグレーション2本 |
| C-4 | **ノートの一括エクスポート**（ZIP・frontmatter付き） | Issue #38 |
| C-5 | **AI使用量計測**（`ai_usage_logs` / `ai_usage_summary(days)`・設定画面の直近30日テーブル） | `20260718000001`。設定画面の1セクションを占めるが、SPEC-dashboard-critique-settings §3.4 のセクション構成にも入っていない |
| C-6 | **グローバルナビ＋エディタ集中モード**（`app/(app)/` ルートグループ化を伴う16ファイル改修） | dev-log セッション㊶（チャット相談から直接処理・Issue非経由） |
| C-7 | **ノートの添付ファイル**（`attachments` バケット・`/api/attachments`） | SPEC-notes §3.2 に追記済み（＝ C ではない・良い前例） |

---

## 3. D: 記述ミス・パス誤り（優先度 低・修正は機械的）

| # | 箇所 | 記述 | 正 |
|---|---|---|---|
| D-1 | SPEC-notes §6 / SPEC-chat-thread-list §5.3・§6 / SPEC-conversational-personas §5.3・§6 / SPEC-schedule-and-memo-tools §6 / SPEC-dashboard-critique-settings §6 | `app/page.tsx` / `app/notes/…` / `app/chats/page.tsx` / `app/settings/page.tsx` | ルートグループ導入後は `app/(app)/…`（C-6 の副作用。SPEC-beat-board 等は更新済み） |
| D-2 | SPEC-beat-board §6 | `lib/schemas/scenes.ts` | シーンのzodは `lib/schemas/projects.ts` に同居（単独ファイルは作られなかった） |
| D-3 | SPEC-proofreading §6 | `lib/schemas/manuscripts.ts` | `lib/schemas/manuscript.ts`（単数形） |
| D-4 | SPEC-auth §7 ほか複数 | `supabase/migrations/XXXX_*.sql` のプレースホルダ | 実ファイル名が確定済み（`20260712000001_auth_allowlist.sql` 等）。SPEC-dashboard-critique-settings §4 の `20260714xxxxxx_` も同様 |
| D-5 | 運用計画 §3 リポジトリ構成 | `SPEC-beatboard.md` / `SPEC-review-profiles.md` | 実在名は `SPEC-beat-board.md`。`SPEC-review-profiles.md` は存在せず、内容は skill `review-profiles` と SPEC-proposal-review に分かれた |
| D-6 | 運用計画 §3 | `.claude/commands/`（スラッシュコマンド） | 存在しない。`/fix-issue` は `.claude/skills/fix-issue/` として実装された |
| D-7 | 運用計画 §3 | hooks 表「migration-guard: 直接書き込みを**ブロック**」 | 実装は `permissionDecision: "ask"`（確認を求める）。ブロックではない |
| D-8 | 運用計画 §3 | CLAUDE.md に含めるもの「猫テーマのトンマナ」 | CLAUDE.md に該当記述なし（意図的に入れなかったなら一覧から削る） |
| D-9 | README §2 環境変数表 / `.env.local.example` | `ENCRYPTION_KEY` = 「**ノート等**の暗号化キー」 | 暗号化対象は **GitHub PAT のみ**（`user_settings.github_pat_ciphertext`）。※締めくくり計画 Step 4 が「RUNBOOK完成と同時に修正」と予定している既知項目。incident-log INC-1 の同種記述も同じ扱い |
| D-10 | README §主な機能 | 5項目 | アトリエ（イラスト生成）・相談（対話型ペルソナ）・スケジュール管理が未掲載（Zenn・原稿連携は記載あり） |

---

## 4. E: 方針判断が必要な事項

| # | 論点 | 選択肢 | 推奨 |
|---|---|---|---|
| E-1 | **SPEC の位置づけ**——「着手前に書いた設計の記録（時点の文書）」か「現在の仕様を映す生きた文書」か | ①時点記録として原則凍結し、冒頭に「後続SPECで上書き」の索引行だけ足す ②現在仕様に追従させて全面改訂 | **①**。SPEC-beat-board が既に採っている「改訂: 日付＋Issue番号＋要点」を全SPECに適用するだけで A-6〜A-13 は解消し、第二期の「着手前に書く」という価値（Step 8 観点C の中核）も損なわない。全面改訂は労力に見合わない |
| E-2 | **実装計画・運用計画の扱い**（A-42〜A-45） | ①末尾に「後日談」節を足すのみ（本文は当時のまま） ②本文を現状へ更新 | **①**。両文書は日付つき改訂履歴を積む形式で書かれており、Step 7・8 の素材としては「当時どう計画したか」が価値。ER図に「※現行スキーマは migrations が正」の1行を足す |
| E-3 | **C（SPEC不在の6機能）に遡ってSPECを書くか** | ①書かない（dev-log とIssueが記録として機能している） ②主要な2〜3件だけ書く ③全件書く | **①**。運用期間かつ Step 3 はドキュメント整備が目的で、遡及SPECは「着手前に書く」という SPEC の定義に反する。代わりに **§5 の対応表に「SPECなし・記録はdev-logセッションN」を明記**して索引を作る |
| E-4 | **要求仕様（A-14〜A-21）の更新範囲** | ①追補節（§9「第二期実装での拡張」）を足す ②本文を書き換える | **①**。要求仕様は Step 8「第一期の問題点6件が解消されたか」の判定基準そのもの。本文を書き換えると比較の基準線が動く。追補節に「汎用執筆支援への拡張」「ペルソナ9人・プロファイル13種への増加」「スコープ外3項目の実装」をまとめる |
| E-5 | **manual.md（A-22〜A-40）をどこで直すか** | ①Step 3 で事実誤りだけ先に直す ②Step 5 の外部向け改訂に全部まとめる | **①＋②**。**A-30・A-33 の2件は「未対応」と書いてある機能が実装済みという明確な誤りなので Step 3 で先に直す**。残りは構成変更を伴うため Step 5 へ送る（本レポートがそのまま作業リストになる） |
| E-6 | **SCHEMA.md 相当の新設**（計画書の判断事項②） | — | **決定済み: 新設しない**（migrations が一次資料。RUNBOOK に必要最小限のみ記載）。今回の調査でも、スキーマの説明を必要とした場面は ER図（A-43）だけで、そこは「migrations が正」の注記で足りる |

---

## 5. CLAUDE.md 棚卸し（現 53 行 → 50 行以内）

計画書 Step 3 の3項目め。現状 **53 行**（計画書作成時 52 行から1行増）で、目標は運用計画 §3 の 50 行以内。

**削減案（-4行）**:

1. **L16 `<!-- スキャフォールド後に実コマンドへ更新すること -->` を削除**（-1行）。Sprint 0 完了時点で役目を終えたコメントが残っている。実コマンドは L18-19 に確定済み
2. **L14-21「検証コマンド」節を3行へ圧縮**（-3行）。見出し＋箇条書き2行＋空行＋説明文の5行構成を、`- 検証: 実装後に必ず \`npm run typecheck\` と \`npm run lint\` を通す` の1行＋見出しに畳む

**追加案（+2行・二失敗ルールの明文化）**:

第二期を通して実際に効いた規律だが、**現在どのドキュメントにも書かれていない**（運用計画 §1-1 の「修正指示が2回失敗したら新規セッション」・§4 フェーズ2 の「修正2回ルール」が最も近いが、CLAUDE.md には無い）。「設計原則」節の直後に置く:

```markdown
## 二失敗ルール

同じ問題に同じアプローチで2回失敗したら、3回目を試さずに手を止めて人間に相談する。
```

差引 **53 - 4 + 2 = 51 行**。50行に収めるにはもう1行削る必要がある。候補は L2 の空行整理、または L46「該当しないルーティンな修正は〜」を L44 のリスト末尾に畳む。**目標50行は「50行前後」の目安として51行を許容するか、上記1行を削るかは判断事項**（運用計画 §3 の趣旨は行数そのものではなく「各行に削除可否を問う」ことにある）。

---

## 6. 実施結果（2026-08-12・全方針は推奨案どおり承認）

E-1=①時点記録＋改訂行 / E-2=①後日談節 / E-3=①索引のみ / E-4=①追補節 / E-5=①誤り2件先行 で承認され、以下を実施した。

| 順 | 対象 | 実施内容 |
|---|---|---|
| 1 | **manual.md の明確な誤り**（A-30・A-32・A-33） | 「ノートの表・画像・コードブロックは非対応」を実際の往復制限の記述へ差し替え。「デフォルトブランチのみ／ブランチ切替は未対応」を §8・§13 の2箇所とも実状（切替・作成・PR作成に対応。入稿ビルドと校正・講評・進捗のみデフォルト固定）へ修正。**残り16件は Step 5 へ申し送り** |
| 2 | **D 記述ミス群** | D-1 ルートグループのパス9件（SPEC 8ファイル）／D-2・D-3 スキーマファイル名／D-4 マイグレーションのプレースホルダ9件を実ファイル名へ。**D-5〜D-8 は運用計画 §7.1 の「計画と実際」表に記録**（E-2 の方針により本文は据え置き）。D-10 は README の主な機能に対話型ペルソナ・アトリエを追加 |
| 3 | **A-1〜A-5 の取り消し表記＋A-6〜A-13 の改訂行** | SPEC-notes / proposal-review / vertical-editor / phase2 / conversational-personas / proofreading / ai-deep-dive / dashboard-critique-settings / refactoring-step1 の9本に「改訂:」行を追加し、実装済みのスコープ外項目に取り消し線＋実装先を明記 |
| 4 | **要求仕様の追補節** | §9「第二期実装での拡張」を新設（9.1 汎用執筆支援 / 9.2 ペルソナ・プロファイルの増加 / 9.3 スコープ外からの昇格3件 / 9.4「読み込み専用」の実際 / 9.5 対応項目のない追加機能）。§1〜§8 は据え置き |
| 5 | **実装計画・運用計画の後日談節** | 実装計画 §8（ER図の現状差・画面一覧の追加分・スケジュールの結果）／運用計画 §7（リポジトリ構成の実際・**§2 対応表の効き目**・§6 のその後）。運用計画 §7.2 は Step 8 の KPT 素材を兼ねる |
| 6 | **CLAUDE.md 棚卸し** | 53行 → **50行**。スキャフォールド時の残骸コメント削除＋検証コマンド節の圧縮（-4行）、設計原則に**二失敗ルール**を明文化（+1行） |
| 7 | **SPEC 索引の新設** | `docs/SPEC-index.md`（SPEC 28本を6分類で一覧＋**SPEC不在で実装した8機能**の記録先＋SPEC以外の設計・運用ドキュメント一覧）。README のドキュメント節からリンク |

### 未対応として送ったもの

- **manual.md の残り16件** → Step 5（外部向けマニュアル改訂）。§1.4 の表が作業リスト
- **D-9 `ENCRYPTION_KEY` の説明ズレ**（README・`.env.local.example`・incident-log INC-1） → Step 4（RUNBOOK 完成と同時に修正する計画済み項目）
- **要求仕様 §3.4 の「読み込み専用」の一文** → 本文据え置きの方針により §9.4 で補足するにとどめた
