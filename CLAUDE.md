# ネコノテAI 第二期

小説執筆支援アプリ。小説は人間が書き、AIはネタ出し・レビュー・校正で伴走する。
原稿の実体はGitHubリポジトリにあり、本アプリはレビュー・進捗管理に専念する。

## 技術スタック

- Next.js（App Router）+ TypeScript（strict）
- UI: shadcn/ui + Tailwind CSS
- DB/認証: Supabase（PostgreSQL + Auth）
- AI: Vercel AI SDK（Anthropic / OpenAI / Google をマルチプロバイダで呼び分け）
- デプロイ: Vercel

## 検証コマンド

<!-- スキャフォールド後に実コマンドへ更新すること -->
- 型チェック: `npm run typecheck`
- リント: `npm run lint`

実装後は必ず typecheck と lint を実行し、エラーを解消してから完了とする。

## 規約

- **テーマ**: 色は必ずテーマ用CSS変数を使う。コンポーネントへの色のハードコード禁止（第一期でテーマ対応に失敗した最重要ルール）
- **スキーマ変更**: 必ずプランモードで承認を得てから、マイグレーションファイル経由で行う。DBへの直接変更は禁止
- **エラー処理**: 共通の `AppError` クラス＋共通ハンドラーを経由させる
- **コミット**: `feat:` / `fix:` / `refactor:` プレフィックス。日本語メッセージ可
- **公開リポジトリ**: 本リポジトリは public。コミット・PR・dev-log 書き戻しに「公開すべきでない情報」を含めない——秘密情報の実値（APIキー・トークン・接続文字列）、実メールアドレス、私的リポジトリ名、未公開作品の実タイトルや原稿からの引用、インフラ識別子（Vercelチームスラッグ・デプロイID）。これらに言及する必要がある場合は既存の伏せ字表記（「Dシリーズ」「セイレーン」「`<owner>/<manuscripts-repo>`」「`<poc-repo>`」「`<team-slug>`」「`dpl_xxx`」等）を維持する。PR作成前に diff を必ずこの観点で確認する

## 設計原則

最もシンプルな方法を選ぶ。頼まれていない抽象化・最適化を追加しない。

## 人間への設計確認が必要な変更（Issue駆動フロー③の判断基準）

以下のいずれかに該当する修正は、実装に着手する前に必ず人間へ設計確認を求める
（詳細は `docs/claude-code-operation-plan.md` フェーズ4）:

- DBスキーマの変更を伴う
- 外部APIの認証フロー変更
- 既存APIのインターフェース変更（破壊的変更）
- セキュリティ関連コードの変更
- 3ファイル以上にまたがる大規模リファクタリング

該当しないルーティンな修正は、確認なしで修正→PR作成まで進めてよい。
本番反映は必ず人間のPRマージ承認を経由する（mainへの直接pushによるデプロイは行わない）。
認証・RLS・秘密情報に触れる変更は、自動フローでも security-reviewer の通過を省略しない。

## SPEC参照

機能の実装時は、対応する `docs/SPEC-*.md` を参照してから着手する。
仕様が存在しない機能は、先に仕様策定（インタビュー → SPEC作成）を提案する。
